From 26243885f28b03e99d8d4d217746eb6177551d03 Mon Sep 17 00:00:00 2001
From: Giulio Benetti <giulio.benetti@micronovasrl.com>
Date: Thu, 21 Dec 2017 19:06:00 +0100
Subject: [PATCH] Added parallel building option in Mali Makefile

Building can be faster

Signed-off-by: Giulio Benetti <giulio.benetti@micronovasrl.com>
---
 src/devicedrv/mali/Makefile | 8 ++++----
 1 file changed, 4 insertions(+), 4 deletions(-)

diff --git a/src/devicedrv/mali/Makefile b/src/devicedrv/mali/Makefile
index 66a2b28..2ee6852 100755
--- a/src/devicedrv/mali/Makefile
+++ b/src/devicedrv/mali/Makefile
@@ -194,18 +194,18 @@ EXTRA_DEFINES += -DMALI_MEM_SWAP_TRACKING=1
 endif
 
 modules: $(UMP_SYMVERS_FILE)
-	$(MAKE) ARCH=$(ARCH) -C $(KDIR) M=$(CURDIR) modules
+	$(MAKE) $(PARALLEL) ARCH=$(ARCH) -C $(KDIR) M=$(CURDIR) modules
 	@rm $(FILES_PREFIX)__malidrv_build_info.c $(FILES_PREFIX)__malidrv_build_info.o
 
 all: modules
 
 clean:
-	$(MAKE) ARCH=$(ARCH) -C $(KDIR) M=$(CURDIR) clean
+	$(MAKE) $(PARALLEL) ARCH=$(ARCH) -C $(KDIR) M=$(CURDIR) clean
 
 install: modules
-	$(MAKE) ARCH=$(ARCH) -C $(KDIR) M=$(CURDIR) INSTALL_MOD_PATH=$(INSTALL_MOD_PATH) modules_install
+	$(MAKE) $(PARALLEL) ARCH=$(ARCH) -C $(KDIR) M=$(CURDIR) INSTALL_MOD_PATH=$(INSTALL_MOD_PATH) modules_install
 
 kernelrelease:
-	$(MAKE) ARCH=$(ARCH) -C $(KDIR) kernelrelease
+	$(MAKE) $(PARALLEL) ARCH=$(ARCH) -C $(KDIR) kernelrelease
 
 export CONFIG KBUILD_EXTRA_SYMBOLS
-- 
2.7.4

